import React from "react";
import "./style.scss";

function StartBtn() {
  return (
    <button className="startBtn">
      <i class="fas fa-plus plus"></i>
      Start a New Journey
    </button>
  );
}

export default StartBtn;
